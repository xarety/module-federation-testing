{
  "name": "module-federation-testing",
  "private": true,
  "scripts": {
    "bootstrap": "npx lerna bootstrap --hoist",
    "start": "lerna run --parallel start",
    "build": "lerna run build",
    "serve": "lerna run --parallel serve",
    "clean": "lerna run --parallel clean"
  },
  "dependencies": {
    "lerna": "^3.22.1"
  }
}
